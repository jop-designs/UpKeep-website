body text: geist mono
-A: Body typography implemented with `Geist Mono` via Google Fonts and CSS variable `--font-body`.

sub title: Quicksand medium
-A: Subtitle typography implemented with `Quicksand` medium via CSS variable `--font-subtitle`.

Main title: work sans medium CAPS
-A: Main titles implemented with `Work Sans` medium and uppercase styling via CSS variable `--font-title`.

use colors used in color_scheme.jpg in the website styling and elements (retrieve hex code for the colors and write them under here)
-A: Extracted palette from `color_scheme.jpg`:
-A: `#0E3F2A` (deep green)
-A: `#277042` (mid green)
-A: `#E68E13` (orange)
-A: `#EDE6D6` (cream)
